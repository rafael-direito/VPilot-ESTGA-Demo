# -*- coding: utf-8 -*-
# @Author: Rafael Direito
# @Date:   2022-10-24 14:17:09
# @Last Modified by:   Rafael Direito
# @Last Modified time: 2022-10-24 21:54:29
version: '3'

volumes:
  postgres_data:
    driver: local

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: testkeycloakdb
      POSTGRES_USER: testkeycloakuser
      POSTGRES_PASSWORD: testkeycloakpassword
    restart:
      always

  keycloak:
    image: jboss/keycloak:18.0.2 
    command:
      - "-b 0.0.0.0 "
    environment:
      DB_VENDOR: POSTGRES
      DB_ADDR: postgres
      DB_DATABASE: testkeycloakdb
      DB_USER: testkeycloakuser
      DB_SCHEMA: public
      DB_PASSWORD: testkeycloakpassword
      KEYCLOAK_USER: keycloakuser
      KEYCLOAK_PASSWORD: keycloakpassword
      PROXY_ADDRESS_FORWARDING: "true"
      KEYCLOAK_LOGLEVEL: DEBUG
      MY_REALM_NAME: vpilot
      MY_REALM_CLIENT: vpilot-client
      MY_REALM_CLIENT_SECRET: dummy_client_secret
      MY_REALM_ADMIN_SECRET: dummy_admin_secret
      MY_REALM_REDIRECT_URI: http://localhost:8000/*
      MY_REALM_ADMIN_USERNAME: dummy_vpilot_admin
      MY_REALM_ADMIN_PASSWORD: dummy_vpilot_password
    ports:
      - '8085:8080'
    depends_on:
      - postgres
    restart:
      always